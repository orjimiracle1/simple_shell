#include "shell.h"

/**
 * print_display - The function takes in the name of the shell prompt
 * @s: the name of the shell to be printed
 * Return: the function returns void
 */
void print_display(char *s)
{
	print_string(s);
}
